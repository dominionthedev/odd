[package]
name = "odd"
version = "0.1.0"
edition = "2021"

# Deliberately minimal. A dependency is added in the same commit as the
# code that first uses it — never ahead of it.

[dependencies]

[dev-dependencies]

[profile.release]
lto = true
